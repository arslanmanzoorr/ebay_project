# Production Environment Variables for Ubuntu Deployment
# Copy this file to .env.prod and update the values
# IMPORTANT: Keep this file secure and private!

# Application Settings
NODE_ENV=production
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Database Settings
POSTGRES_DB=auctionflow
POSTGRES_USER=auctionuser
POSTGRES_PASSWORD=auctionpass123-change-this
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Redis Settings
REDIS_PASSWORD=redispass123-change-this
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379

# Security Settings
# Generate a strong secret key: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# n8n Webhook Settings
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id

# Admin Credentials (change after first deployment)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# Production Deployment Settings
DOMAIN=your-domain.com
SSL_EMAIL=admin@your-domain.com

# Security Notes:
# - Change POSTGRES_PASSWORD to a strong password
# - Change REDIS_PASSWORD to a strong password
# - Change JWT_SECRET to a unique value
# - Change ADMIN_PASSWORD after first login
# - Update DOMAIN and SSL_EMAIL for your domain
# - Keep this file secure and private
