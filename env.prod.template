# Production Environment Variables
# Copy this file to .env.prod and update the values
# IMPORTANT: Keep admin-credentials.txt secure and private!

# Django Settings
DEBUG=False
SECRET_KEY=your-super-secret-key-change-this-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,your-domain.com

# Database Settings
POSTGRES_DB=auctionflow
POSTGRES_USER=auctionuser
POSTGRES_PASSWORD=auctionpass123-change-this
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Redis Settings
REDIS_URL=redis://redis:6379

# Frontend Settings
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://your-domain.com

# Admin Credentials (see admin-credentials.txt for secure values)
ADMIN_USERNAME=auctionflow_admin
ADMIN_EMAIL=admin@auctionflow.com
ADMIN_PASSWORD=AuCtIoNfLoW_2024_S3cur3!

# n8n Webhook URLs (update these with your actual n8n instance)
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id

# Security Notes:
# - Change SECRET_KEY to a unique value
# - Change POSTGRES_PASSWORD to a strong password
# - Change ADMIN_PASSWORD after first login
# - Keep admin-credentials.txt secure and private
